import React, { useState, useEffect } from 'react'

const Row = ({ title, fetchURL }) => {
  const [movies, setMovies] = useState([])

  useEffect(() => {
    effect
    return () => {
      cleanup
    }
  }, [input])

  return (
    <div>
      <h2>{title}</h2>
    </div>
  )
}

export default Row
